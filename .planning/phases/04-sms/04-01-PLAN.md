---
phase: 04-sms
plan: 01
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - prisma/schema.prisma
  - src/modules/sms/sms.service.ts
  - src/modules/sms/sms.controller.ts
  - src/modules/sms/sms.routes.ts
autonomous: true
requirements:
  - SMS-07
must_haves:
  truths:
    - "同一手机号60秒内不能重复发送验证码"
    - "连续5次验证失败锁定该手机号15分钟"
    - "短信发送行为记录到审计日志（手机号前3位+后4位、操作结果）"
    - "系统提供短信发送统计接口（当天发送次数）"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "SmsAuditLog模型存储审计日志"
      contains: "model SmsAuditLog"
    - path: "src/modules/sms/sms.service.ts"
      provides: "安全相关Redis操作和审计日志函数"
      exports: "canSendSms,isPhoneLocked,incrementFailCount,resetFailCount,getTodaySendCount,maskPhone,createAuditLog"
    - path: "src/modules/sms/sms.controller.ts"
      provides: "集成安全检查的SMS控制器"
      contains: "安全检查逻辑"
    - path: "src/modules/sms/sms.routes.ts"
      provides: "短信统计接口"
      routes: "GET /stats"
  key_links:
    - from: "src/modules/sms/sms.controller.ts"
      to: "prisma/schema.prisma"
      via: "prisma.smsAuditLog.create"
    - from: "src/modules/sms/sms.controller.ts"
      to: "src/modules/sms/sms.service.ts"
      via: "canSendSms,isPhoneLocked,incrementFailCount,resetFailCount,getTodaySendCount,createAuditLog"
---

<objective>
实现SMS安全加固：60秒发送限制、5次失败锁定、审计日志、发送统计。
</objective>

<purpose>
防止SMS轰炸攻击和验证码暴力破解，满足SMS-07要求，并记录完整审计日志供后续分析。
</purpose>

<context>
@src/modules/sms/sms.service.ts
@src/modules/sms/sms.controller.ts
@src/modules/sms/sms.routes.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 SmsAuditLog Prisma 模型</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>
    在 prisma/schema.prisma 的末尾添加 SmsAuditLog 模型:

    ```prisma
    model SmsAuditLog {
      id        String   @id @default(cuid())
      phone     String   // 格式: 138****5678 (掩码手机号)
      action    String   // 'send' | 'verify_success' | 'verify_fail' | 'locked'
      result    String   // 'success' | 'failed' | 'rate_limited' | 'locked'
      ip        String?  @map("ip")
      userAgent String?  @map("user_agent")
      createdAt DateTime @default(now()) @map("created_at")

      @@index([phone, createdAt])
      @@index([action, createdAt])
      @@map("sms_audit_logs")
    }
    ```

    然后运行 prisma migrate 或 prisma db push 生成客户端:
    ```bash
    npx prisma generate
    ```
  </action>
  <acceptance_criteria>
    - schema.prisma 包含 model SmsAuditLog
    - SmsAuditLog 包含 phone, action, result, ip, userAgent, createdAt 字段
    - SmsAuditLog 有正确的索引 @@index([phone, createdAt])
    - prisma generate 成功执行无错误
  </acceptance_criteria>
  <verify>
    <automated>grep -c "model SmsAuditLog" prisma/schema.prisma && grep -c "@@index(\[phone, createdAt\])" prisma/schema.prisma && npx prisma generate --quiet 2>&1 | grep -c "generated client"</automated>
  </verify>
  <done>SmsAuditLog 模型已创建并生成 Prisma 客户端</done>
</task>

<task type="auto">
  <name>Task 2: 添加安全相关函数到 sms.service.ts</name>
  <files>src/modules/sms/sms.service.ts</files>
  <read_first>src/modules/sms/sms.service.ts</read_first>
  <action>
    在 sms.service.ts 文件末尾添加以下安全相关函数:

    1. Redis key 前缀常量:
    ```typescript
    // 安全相关 Redis key 前缀
    export const SMS_LAST_SEND_PREFIX = 'sms:last_send:';      // 60秒发送限制
    export const SMS_FAIL_COUNT_PREFIX = 'sms:fail_count:';    // 失败次数计数
    export const SMS_LOCKED_PREFIX = 'sms:locked:';            // 锁定标志
    export const SMS_SEND_COUNT_PREFIX = 'sms:send_count:';    // 每日发送统计

    const MAX_FAIL_ATTEMPTS = 5;
    const LOCK_DURATION_SECONDS = 900; // 15分钟
    ```

    2. 手机号掩码函数:
    ```typescript
    /**
     * 掩码手机号: 13812345678 -> 138****5678
     */
    export function maskPhone(phone: string): string {
      if (phone.length < 7) return '****';
      return `${phone.slice(0, 3)}****${phone.slice(-4)}`;
    }
    ```

    3. 60秒发送限制函数:
    ```typescript
    /**
     * 检查手机号是否可以发送短信 (60秒限制)
     * @returns true 表示可以发送，false 表示被限流
     */
    export async function canSendSms(phone: string): Promise<boolean> {
      const key = `${SMS_LAST_SEND_PREFIX}${phone}`;
      const lastSend = await redis.get(key);
      return !lastSend;
    }

    /**
     * 记录短信发送时间 (设置60秒TTL)
     */
    export async function recordSmsSend(phone: string): Promise<void> {
      const key = `${SMS_LAST_SEND_PREFIX}${phone}`;
      await redis.set(key, Date.now().toString(), 'EX', 60);
    }
    ```

    4. 失败锁定相关函数:
    ```typescript
    /**
     * 检查手机号是否被锁定
     * @returns true 表示被锁定，false 表示正常
     */
    export async function isPhoneLocked(phone: string): Promise<boolean> {
      const key = `${SMS_LOCKED_PREFIX}${phone}`;
      const locked = await redis.get(key);
      return locked === '1';
    }

    /**
     * 增加失败计数，达到5次时锁定15分钟
     * @returns true 表示刚刚被锁定，false 表示还未锁定
     */
    export async function incrementFailCount(phone: string): Promise<boolean> {
      const countKey = `${SMS_FAIL_COUNT_PREFIX}${phone}`;
      const lockKey = `${SMS_LOCKED_PREFIX}${phone}`;

      // 原子操作: INCR 和 EXPIRE
      const pipeline = redis.multi();
      pipeline.incr(countKey);
      pipeline.expire(countKey, LOCK_DURATION_SECONDS);
      const results = await pipeline.exec();

      const count = results?.[0]?.[1] as number;
      if (count >= MAX_FAIL_ATTEMPTS) {
        // 锁定手机号
        await redis.set(lockKey, '1', 'EX', LOCK_DURATION_SECONDS);
        await redis.del(countKey); // 锁定后重置计数器
        return true; // 已锁定
      }
      return false; // 未锁定
    }

    /**
     * 验证成功后重置失败计数
     */
    export async function resetFailCount(phone: string): Promise<void> {
      const countKey = `${SMS_FAIL_COUNT_PREFIX}${phone}`;
      await redis.del(countKey);
    }
    ```

    5. 发送统计相关函数:
    ```typescript
    /**
     * 获取当天发送次数
     */
    export async function getTodaySendCount(phone: string): Promise<number> {
      const today = new Date().toISOString().slice(0, 10).replace(/-/g, '');
      const key = `${SMS_SEND_COUNT_PREFIX}${phone}:${today}`;
      const count = await redis.get(key);
      return count ? parseInt(count, 10) : 0;
    }

    /**
     * 增加发送计数并返回当天总数
     */
    export async function incrementSendCount(phone: string): Promise<number> {
      const today = new Date().toISOString().slice(0, 10).replace(/-/g, '');
      const key = `${SMS_SEND_COUNT_PREFIX}${phone}:${today}`;
      const pipeline = redis.multi();
      pipeline.incr(key);
      pipeline.expire(key, 86400); // 24小时
      const results = await pipeline.exec();
      return results?.[0]?.[1] as number ?? 0;
    }
    ```
  </action>
  <acceptance_criteria>
    - 导出 SMS_LAST_SEND_PREFIX, SMS_FAIL_COUNT_PREFIX, SMS_LOCKED_PREFIX, SMS_SEND_COUNT_PREFIX
    - 导出 canSendSms, recordSmsSend, isPhoneLocked, incrementFailCount, resetFailCount, getTodaySendCount, incrementSendCount, maskPhone 函数
    - canSendSms 使用 SET key value EX 60 NX 模式实现原子性
    - incrementFailCount 使用 Redis MULTI/EXEC 保证原子性
  </acceptance_criteria>
  <verify>
    <automated>grep -c "export async function canSendSms" src/modules/sms/sms.service.ts && grep -c "export async function isPhoneLocked" src/modules/sms/sms.service.ts && grep -c "export async function incrementFailCount" src/modules/sms/sms.service.ts && grep -c "export function maskPhone" src/modules/sms/sms.service.ts && grep -c "export async function getTodaySendCount" src/modules/sms/sms.service.ts</automated>
  </verify>
  <done>sms.service.ts 包含所有安全相关函数</done>
</task>

<task type="auto">
  <name>Task 3: 更新 sms.controller.ts 集成安全检查</name>
  <files>src/modules/sms/sms.controller.ts</files>
  <read_first>src/modules/sms/sms.controller.ts</read_first>
  <action>
    更新 src/modules/sms/sms.controller.ts:

    1. 更新导入，添加新的服务函数和 Prisma:
    ```typescript
    import { createAndStoreSmsCode, getSmsCode, deleteSmsCode,
      canSendSms, recordSmsSend,
      isPhoneLocked, incrementFailCount, resetFailCount,
      getTodaySendCount, incrementSendCount,
      maskPhone } from './sms.service.js';
    import prisma from '../../lib/prisma.js';
    ```

    2. 添加审计日志辅助函数:
    ```typescript
    async function createAuditLog(params: {
      phone: string;
      action: 'send' | 'verify_success' | 'verify_fail' | 'locked';
      result: 'success' | 'failed' | 'rate_limited' | 'locked';
      ip?: string;
      userAgent?: string;
    }): Promise<void> {
      const maskedPhone = maskPhone(params.phone);
      await prisma.smsAuditLog.create({
        data: {
          phone: maskedPhone,
          action: params.action,
          result: params.result,
          ip: params.ip,
          userAgent: params.userAgent,
        },
      });
    }
    ```

    3. 更新 sendSmsCode 处理器，在生成验证码前添加60秒限制检查:
    ```typescript
    export const sendSmsCode = async (req: Request, res: Response): Promise<void> => {
      try {
        const { phone, type } = req.body;
        const ip = req.ip;
        const userAgent = req.get('user-agent');

        // ... 验证逻辑保持不变 ...

        // 60秒发送限制检查
        const allowed = await canSendSms(phone);
        if (!allowed) {
          await createAuditLog({ phone, action: 'send', result: 'rate_limited', ip, userAgent });
          res.status(429).json({ error: '发送过于频繁，请稍后再试' });
          return;
        }

        // 生成并存储验证码
        const code = await createAndStoreSmsCode(phone, type);

        // 发送短信
        const result = await sendSms(phone, code);

        if (!result.success) {
          console.error('SMS send failed:', result.error);
          await createAuditLog({ phone, action: 'send', result: 'failed', ip, userAgent });
          res.status(500).json({ error: '短信发送失败' });
          return;
        }

        // 记录发送时间、增量统计、创建审计日志
        await recordSmsSend(phone);
        await incrementSendCount(phone);
        await createAuditLog({ phone, action: 'send', result: 'success', ip, userAgent });

        // 返回通用响应
        res.status(200).json({ success: true, message: '验证码已发送' });
      } catch (error) {
        console.error('Send SMS error:', error);
        res.status(500).json({ error: '服务器错误' });
      }
    };
    ```

    4. 更新 smsLogin 处理器，添加锁定检查和失败计数:
    ```typescript
    export const smsLogin = async (req: Request, res: Response): Promise<void> => {
      try {
        const { phone, code } = req.body;
        const ip = req.ip;
        const userAgent = req.get('user-agent');

        // ... 验证逻辑保持不变 ...

        // 检查手机号是否被锁定
        const locked = await isPhoneLocked(phone);
        if (locked) {
          await createAuditLog({ phone, action: 'verify_fail', result: 'locked', ip, userAgent });
          res.status(429).json({ error: '验证码错误' }); // 通用错误信息
          return;
        }

        // 验证验证码
        const storedCode = await getSmsCode(phone, 'login');
        if (!storedCode || storedCode !== code) {
          // 增加失败计数
          const nowLocked = await incrementFailCount(phone);
          await createAuditLog({ phone, action: 'verify_fail', result: 'failed', ip, userAgent });

          if (nowLocked) {
            await createAuditLog({ phone, action: 'locked', result: 'locked', ip, userAgent });
          }

          res.status(401).json({ error: '验证码错误' });
          return;
        }

        // 验证成功：重置失败计数，删除验证码
        await resetFailCount(phone);
        await deleteSmsCode(phone, 'login');
        await createAuditLog({ phone, action: 'verify_success', result: 'success', ip, userAgent });

        // ... 后续登录逻辑保持不变 ...
      } catch (error) {
        console.error('SMS login error:', error);
        res.status(500).json({ error: '服务器错误' });
      }
    };
    ```

    5. 更新 smsRegister 处理器，添加相同的锁定检查和失败计数:
    ```typescript
    export const smsRegister = async (req: Request, res: Response): Promise<void> => {
      try {
        const { phone, code, username } = req.body;
        const ip = req.ip;
        const userAgent = req.get('user-agent');

        // ... 验证逻辑保持不变 ...

        // 检查手机号是否被锁定
        const locked = await isPhoneLocked(phone);
        if (locked) {
          await createAuditLog({ phone, action: 'verify_fail', result: 'locked', ip, userAgent });
          res.status(429).json({ error: '验证码错误' });
          return;
        }

        // 验证验证码
        const storedCode = await getSmsCode(phone, 'register');
        if (!storedCode || storedCode !== code) {
          // 增加失败计数
          const nowLocked = await incrementFailCount(phone);
          await createAuditLog({ phone, action: 'verify_fail', result: 'failed', ip, userAgent });

          if (nowLocked) {
            await createAuditLog({ phone, action: 'locked', result: 'locked', ip, userAgent });
          }

          res.status(401).json({ error: '验证码错误' });
          return;
        }

        // 验证成功：重置失败计数，删除验证码
        await resetFailCount(phone);
        await deleteSmsCode(phone, 'register');
        await createAuditLog({ phone, action: 'verify_success', result: 'success', ip, userAgent });

        // ... 后续注册逻辑保持不变 ...
      } catch (error) {
        console.error('SMS register error:', error);
        res.status(500).json({ error: '服务器错误' });
      }
    };
    ```
  </action>
  <acceptance_criteria>
    - sms.controller.ts 导入新的安全相关函数
    - sendSmsCode 调用 canSendSms 检查，失败返回 429
    - sendSmsCode 调用 recordSmsSend, incrementSendCount, createAuditLog
    - smsLogin 和 smsRegister 调用 isPhoneLocked 检查锁定状态
    - smsLogin 和 smsRegister 调用 incrementFailCount 增加失败计数
    - smsLogin 和 smsRegister 调用 resetFailCount 重置计数（验证成功时）
    - smsLogin 和 smsRegister 调用 createAuditLog 记录审计日志
    - 锁定时使用通用错误信息 "验证码错误"
  </acceptance_criteria>
  <verify>
    <automated>grep -c "canSendSms" src/modules/sms/sms.controller.ts && grep -c "isPhoneLocked" src/modules/sms/sms.controller.ts && grep -c "incrementFailCount" src/modules/sms/sms.controller.ts && grep -c "createAuditLog" src/modules/sms/sms.controller.ts && grep -c "resetFailCount" src/modules/sms/sms.controller.ts</automated>
  </verify>
  <done>sms.controller.ts 集成所有安全检查</done>
</task>

<task type="auto">
  <name>Task 4: 添加统计接口到 sms.routes.ts</name>
  <files>src/modules/sms/sms.routes.ts</files>
  <read_first>src/modules/sms/sms.routes.ts</read_first>
  <action>
    更新 src/modules/sms/sms.routes.ts:

    1. 添加导入:
    ```typescript
    import { sendSmsCode, smsLogin, smsRegister, getSmsStats } from './sms.controller.js';
    ```

    2. 添加统计路由:
    ```typescript
    /**
     * GET /auth/sms/stats
     * 查询当天短信发送统计
     * Query: { phone: string }
     * Returns: { phone: string (masked), todaySendCount: number, date: string }
     */
    router.get('/stats', getSmsStats);
    ```

    在 sms.controller.ts 中添加 getSmsStats 处理器:
    ```typescript
    /**
     * GET /auth/sms/stats
     * 查询当天短信发送统计
     */
    export const getSmsStats = async (req: Request, res: Response): Promise<void> => {
      try {
        const { phone } = req.query;

        if (!phone || !isValidChinesePhone(phone as string)) {
          res.status(400).json({ error: '无效的手机号' });
          return;
        }

        const todayCount = await getTodaySendCount(phone as string);

        res.status(200).json({
          phone: maskPhone(phone as string),
          todaySendCount: todayCount,
          date: new Date().toISOString().slice(0, 10),
        });
      } catch (error) {
        console.error('Get SMS stats error:', error);
        res.status(500).json({ error: '服务器错误' });
      }
    };
    ```
  </action>
  <acceptance_criteria>
    - sms.routes.ts 添加 GET /stats 路由
    - getSmsStats 函数验证手机号格式
    - getSmsStats 返回掩码手机号、当天发送次数、日期
  </acceptance_criteria>
  <verify>
    <automated>grep -c "router.get('/stats'" src/modules/sms/sms.routes.ts && grep -c "export const getSmsStats" src/modules/sms/sms.controller.ts</automated>
  </verify>
  <done>统计接口 GET /auth/sms/stats 已添加</done>
</task>

</tasks>

<verification>
- 60秒发送限制: canSendSms 返回 false 时返回 429 错误
- 5次失败锁定: incrementFailCount 达到5次时设置锁定标志
- 审计日志: 每次 send/verify_success/verify_fail/locked 都有记录
- 发送统计: getTodaySendCount 返回当天发送次数
- 统计接口: GET /auth/sms/stats?phone=xxx 返回 {phone, todaySendCount, date}
</verification>

<success_criteria>
- 同一手机号60秒内重复发送返回 429 "发送过于频繁，请稍后再试"
- 5次验证失败后手机号被锁定15分钟，返回通用错误 "验证码错误"
- 所有SMS操作记录到 sms_audit_logs 表（手机号已掩码）
- GET /auth/sms/stats 接口返回当天发送统计
</success_criteria>

<output>
After completion, create `.planning/phases/04-sms/04-01-SUMMARY.md`
</output>
