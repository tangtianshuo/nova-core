# npm package configuration
# This file configures how the package is published

# Use the public registry (default)
registry=https://registry.npmjs.org/

# For private packages, you would use:
# registry=https://npm.your-company.com/

# Always authenticate when publishing
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
